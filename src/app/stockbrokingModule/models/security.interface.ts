/**
 * The interface for a security
 *
 * @export
 * @interface ISecurity
 */
export interface ISecurity {
  id: string;
  name: string;
  label: string;
  sector: string;
  lastTradePrice: string;
}
